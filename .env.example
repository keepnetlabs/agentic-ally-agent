# Agentic Ally Environment Configuration
# Copy to .env and fill in values. NEVER commit .env with real credentials.
#
# Convention: REQUIRED = must be set for deployment | OPTIONAL = has fallback
# Validation: src/mastra/utils/core/env-validation.ts

# ============================================================================
# CLOUDFLARE CONFIGURATION [REQUIRED]
# ============================================================================

# Your Cloudflare account ID
# Find at: https://dash.cloudflare.com/profile/api-tokens (Account ID field)
CLOUDFLARE_ACCOUNT_ID=your-cloudflare-account-id

# Cloudflare API key for KV access
# Create at: https://dash.cloudflare.com/profile/api-tokens
CLOUDFLARE_API_KEY=your-cloudflare-api-key

# Cloudflare AI Gateway ID for model routing
# Create at: https://dash.cloudflare.com/ai-gateway
CLOUDFLARE_AI_GATEWAY_ID=your-ai-gateway-id

# Authentication key for Cloudflare AI Gateway
CLOUDFLARE_GATEWAY_AUTHENTICATION_KEY=your-gateway-auth-key

# Cloudflare KV token with KV:read and KV:write permissions
# Create at: https://dash.cloudflare.com/profile/api-tokens (KV token scope)
CLOUDFLARE_KV_TOKEN=your-kv-api-token

# Cloudflare D1 Database ID for embeddings cache
# Find at: https://dash.cloudflare.com (D1 section)
CLOUDFLARE_D1_DATABASE_ID=your-d1-database-id

# ============================================================================
# KV NAMESPACES [OPTIONAL - Defaults in constants.ts]
# ============================================================================

# Microlearning KV Namespace ID
MICROLEARNING_KV_NAMESPACE_ID=your-microlearning-kv-id

# Phishing KV Namespace ID
PHISHING_KV_NAMESPACE_ID=your-phishing-kv-id

# Smishing KV Namespace ID
SMISHING_KV_NAMESPACE_ID=your-smishing-kv-id

# ============================================================================
# WORKER ENDPOINTS [OPTIONAL - Fallbacks in constants.ts]
# ============================================================================

# Phishing worker endpoints (fallback in constants.ts)
PHISHING_WORKER_URL=https://your-phishing-worker.workers.dev/submit
PHISHING_WORKER_SEND=https://your-phishing-worker.workers.dev/send

# Training worker endpoints
TRAINING_WORKER_URL=https://your-training-worker.workers.dev/submit
TRAINING_WORKER_SEND=https://your-training-worker.workers.dev/send

# Smishing worker endpoints (if not set, falls back to phishing worker)
SMISHING_WORKER_URL=https://your-smishing-worker.workers.dev/submit
SMISHING_WORKER_SEND=https://your-smishing-worker.workers.dev/send

# ============================================================================
# ELEVENLABS (VISHING) [OPTIONAL - Required only for /vishing/* endpoints]
# ============================================================================

# ElevenLabs API key for Conversational AI (outbound vishing calls)
# Create at: https://elevenlabs.io/app/settings/api-keys
ELEVENLABS_API_KEY=your-elevenlabs-api-key

# ElevenLabs Conversational AI agent ID (from your ElevenLabs project)
ELEVENLABS_AGENT_ID=your-agent-id

# ============================================================================
# HEYGEN (DEEPFAKE VIDEO) [OPTIONAL - Required only for deepfake video generation]
# ============================================================================

# HeyGen API key for AI video generation (deepfake simulations)
# Create at: https://app.heygen.com/settings/api-key
HEYGEN_API_KEY=your-heygen-api-key

# ============================================================================
# LLM PROVIDERS [REQUIRED]
# ============================================================================

# OpenAI API key for gpt-4o-mini (agent) and gpt-oss-120b routing
# Create at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-your-openai-api-key-here

# ============================================================================
# LLM PROVIDERS [OPTIONAL]
# ============================================================================

# Google Generative AI API key (alternative provider)
# Create at: https://makersuite.google.com/app/apikey
GOOGLE_GENERATIVE_AI_API_KEY=your-google-api-key

# ============================================================================
# MASTRA MEMORY [OPTIONAL]
# ============================================================================

# Turso/LibSQL connection string for agent memory
# Format: libsql://your-database.turso.io
MASTRA_MEMORY_URL=libsql://your-database-url

# Turso API token for database access
# Create at: https://app.turso.io
MASTRA_MEMORY_TOKEN=your-turso-auth-token

# ============================================================================
# PRODUCT API [REQUIRED for whitelabeling/integrations]
# ============================================================================

# Logo.dev API Token (Primary Logo Source)
# Get your token from https://logo.dev/
LOGO_DEV_TOKEN=LOGO_DEV_PUBLISHABLE_KEY

# API Key for accessing the main product backend
# Used for whitelabeling and other integrations
PRODUCT_API_KEY=your-product-api-key

# Product API base URL (optional, defaults to empty)
PRODUCT_API_URL=https://api.your-product.com

# ============================================================================
# REMOTE STORAGE [OPTIONAL - Backup when KV unavailable]
# ============================================================================

# Remote API endpoint for backup microlearning persistence
# Used when KV storage is unavailable
REMOTE_STORAGE_URL=https://your-backup-api.workers.dev/microlearning

# ============================================================================
# RUNTIME [OPTIONAL]
# ============================================================================

# Node environment (development|test|production)
NODE_ENV=development

# Log level: debug | info | warn | error (overrides NODE_ENV default)
# Structured logs include: service=agentic-ally, env, correlationId (Datadog/Sentry ready)
LOG_LEVEL=info

# ============================================================================
# OBSERVABILITY (SENTRY) [OPTIONAL]
# ============================================================================

# Sentry DSN for error tracking (https://sentry.io)
SENTRY_DSN=

# Sentry environment (production|staging|development)
SENTRY_ENVIRONMENT=production
# ============================================================================
# SECURITY NOTES
# ============================================================================
#
# 1. NEVER commit this file with real credentials
# 2. Add .env to .gitignore (it is)
# 3. Rotate all credentials immediately if exposed
# 4. Use strong, unique tokens for production
# 5. Consider using a secrets manager (Cloudflare Secrets, 1Password, etc.)
#
